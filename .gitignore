*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Release signing. A keystore and its passwords are credentials, not source, and losing
# control of them means losing control of app updates. Listed before one exists so the
# mistake cannot be made in the first place.
*.jks
*.keystore
keystore.properties
